# Selective recommendation via the frozen MARIUS likelihood (a positive result)

Generated by `scripts/extensions/selective_prediction.py` (zero-GPU, read-only, seed 42,
the only seed with committed depth-100 scored dumps). All numbers below are reproducible:

    python scripts/extensions/selective_prediction.py

## The result

MARIUS's autoregressive decode produces a NORMALIZED joint code-tuple log-probability that
is comparable across users, so it doubles as a per-user reliability signal. Ranking users by
that confidence and abstaining on the low-confidence tail gives a steep selective-accuracy gain.

| dataset | base Hit@10 | top-25% | top-10% | top-5% | lift@5% | AURC(miss@10) vs random |
|---|---|---|---|---|---|---|
| Beauty  | 0.0850 | 0.1697 | 0.2308 | 0.2800 | 3.29x | 0.8543 < 0.9150 |
| Sports  | 0.0464 | 0.0818 | 0.1087 | 0.1236 | 2.67x | 0.9275 < 0.9536 |

(Selective-NDCG@10 follows the same monotone shape; see `<cat>_summary.json`.)

NOTE on the denominator: "base Hit@10" here (Beauty 0.0850, Sports 0.0464) is the model's
catalog-reach-collapsed top-10 output on these dumps (consistent with the depth-20 dumps,
0.0848 / 0.0463). It is the honest, conservative denominator for the selective story and is
NOT the paper-metric R@10 reported in REPLICATION_REPORT.md; do not conflate the two in the
writeup.

This is a capability the discriminative SASRec++ baseline does not provide by construction:
its dot-product score is unnormalized and is not a per-user reliability scale. It turns the
reproduction context "MARIUS underperforms SASRec on raw Beauty recall" into an asset:
MARIUS loses on raw recall but knows WHEN it is right, enabling selective serving / a
human-handoff or fallback on the low-confidence tail.

## Confound battery (the load-bearing defense; all pass on both datasets)

1. NOT a history-length proxy: corr(confidence, history length) = -0.003 (Beauty) / -0.153
   (Sports); the confidence router dominates a history-length router at every coverage.
2. Per-instance difficulty, not a cold/warm sort: the selective lift holds WITHIN each
   history-length bucket (cold <=5, mid, warm >=16); see the `within_history_bucket` block.
3. NOT a popularity proxy (the key check): corr(confidence, log target-popularity) = 0.038
   (Beauty) / -0.056 (Sports), and the selective lift on TAIL targets EXCEEDS the lift on
   HEAD targets (Beauty 3.61x tail vs 2.46x head; Sports 5.52x vs 2.23x). Absolute
   selective-Hit@10 at 10% coverage (base -> selective): Beauty tail 0.033 -> 0.119, head
   0.136 -> 0.335; Sports tail 0.008 -> 0.046, head 0.084 -> 0.188. Caveat: the tail RATIO is
   amplified by a small tail base (esp. Sports tail base 0.008), so report the absolutes too;
   the point stands either way -- confidence helps tail targets at least as much as head, so
   this is not a restatement of the popularity/catalog-collapse story.
4. The top-1 log-prob beats the margin (top1 - top2) signal at every coverage.

## Honest bounds (do not oversell)

- Single seed (seed 42 is the only seed with scored depth-100 dumps). Error bars require a
  cheap MARIUS `--with-scores` re-dump for seeds 43/44.
- The cross-paradigm HEAD-TO-HEAD ("the frozen likelihood beats SASRec at matched answered
  coverage") is NOT computed here: SASRec scores are not on disk (its dump has only
  topk_items/target/hist_len). It needs a cheap SASRec `--with-scores` re-dump, and because
  MARIUS's absolute recall is lower it may come out flat. The headline is the calibrated,
  training-free reliability CAPABILITY and the steeper selective slope, NOT a raw-accuracy win.

## Novelty positioning (PARTLY-OPEN)

- UGR (arXiv:2602.11719, KDD 2026) publishes abstention / risk-coverage on a semantic-ID
  generative recommender, but via a TRAINED confidence token (GRPO), with no frozen-likelihood
  angle, no discriminative comparison, no AURC, and no confound controls. Differentiate hard on:
  (a) the FROZEN decode log-likelihood (zero extra training), (b) the generative-vs-discriminative
  framing, (c) the confound battery above.
- Cite also: our own depth-100 conformal pilot (a different, set-size question), CPFT
  (arXiv:2402.08976, conformal fine-tuning of a discriminative recommender), USD (arXiv:2508.07210).
- Selective prediction / reject-option / AURC is a mature classification paradigm; this is an
  honest cross-domain port to the generative recommender setting.
